# first.lang

import {
    compiler: "/std/compiler.lang",
    console: "/std/console.lang",
    Eg2: "eg2.lang"
};

cwd = (it: string) -> f"src/eg1/{it}";

Main = (args: Slc<string>) -> {
    # Implement stuff
};
