mod test_module {

    let OptionalInt32 = enum {
        Some Int32,
        None
    };

    let divide (p Int32, q Int32) OptionalInt32 = {
        if (q == 0) {
            OptionalInt32::None
        } else {
            OptionalInt32::Some using (p / q)
        }
    };

};
