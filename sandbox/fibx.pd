import {
    raylib from "./raylib.pd-extern.json"
}

mod fib_helper {
    let register (x Int32, y Int32) Void = {
        raylib:init_window();

        # todo: display a window with a message using RayLib

        raylib:close_window();
    };
};
